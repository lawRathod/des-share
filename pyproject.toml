[project]
name = "des-share"
version = "0.1.0"
description = "A simple Streamlit app template."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pandas>=2.0.0",
    "streamlit>=1.60.0",
]

[tool.uv]
package = false
